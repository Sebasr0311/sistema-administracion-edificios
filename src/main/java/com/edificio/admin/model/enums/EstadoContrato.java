package com.edificio.admin.model.enums;

/** Estados del ciclo de vida de un contrato (tabla CONTRATOS.estado). */
public enum EstadoContrato {
    PENDIENTE_FIRMA,
    ACTIVO,
    VENCIDO,
    CANCELADO,
    SUSPENDIDO
}
