package com.edificio.admin.model.enums;

/** Estados de una cuota de arriendo (tabla CUOTAS_ARRIENDO.estado). */
public enum EstadoCuota {
    PENDIENTE,
    PAGADA,
    VENCIDA,
    EN_MORA,
    ANULADA
}
